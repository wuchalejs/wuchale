{
    "name": "wuchale",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "tsc --build --watch",
        "build": "tsc --build",
        "test": "npm run test --workspaces --if-present"
    },
    "workspaces": [ "packages/*" ]
}
