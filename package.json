{
    "name": "wuchale",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "tsc --build --watch",
        "build": "tsc --build",
        "test": "npm run test --workspaces",
        "v": "npx @changesets/cli"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@changesets/cli": "^2.29.5"
    }
}
