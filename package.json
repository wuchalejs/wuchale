{
    "name": "wuchale",
    "type": "module",
    "private": true,
    "scripts": {
        "clean": "rm -r packages/*/dist packages/*/*.tsbuildinfo packages/*/*/*.tsbuildinfo",
        "dev": "npm run clean && tsc --build tsconfig.build.json --watch",
        "build": "tsc --build tsconfig.build.json --declarationMap false",
        "test": "npm run test --workspaces",
        "v": "npx @changesets/cli"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@changesets/cli": "^2.29.6"
    }
}
